<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StylesDemo.MainPage">
    <ContentPage.Resources>

        <!--#region NewRegion -->
        <Style TargetType="VerticalStackLayout">
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="Padding" Value="30,0" />
            <Setter Property="Spacing" Value="25" />
        </Style>
        <!--#endregion-->
        
        <!--#region Aplicación de estilos a componentes derivados -->
        <!--
        <Style TargetType="StackBase" ApplyToDerivedTypes="True">
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="Padding" Value="30,0" />
            <Setter Property="Spacing" Value="25" />
        </Style>
        -->
        <!--#endregion-->

        
    </ContentPage.Resources>
    
    <ScrollView>
        <VerticalStackLayout>
            <Button Text="LOGIN" />
            <Button Style="{StaticResource SpecialButton}" Text="VISIT WEB SITE" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
